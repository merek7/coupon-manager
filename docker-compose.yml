services:
  app:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - coupon_data:/data
    environment:
      - DATABASE_PATH=/data/coupons.db
      - PORT=5000
    restart: unless-stopped

volumes:
  coupon_data:
